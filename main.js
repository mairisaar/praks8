//DOM Elements
let numInput = document.getElementById('numInput');

//Listening to events
numInput.addEventListener('input', e => {
    let num = e.target.value;
    
    result = console.log('ter');
});